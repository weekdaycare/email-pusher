{
  "name": "rss-article-notifier",
  "version": "1.0.0",
  "description": "A GitHub Action to fetch RSS feeds, detect new articles, and send email notifications.",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "keywords": [
    "github-action",
    "rss",
    "email",
    "notifier",
    "automation"
  ],
  "license": "MIT",
  "dependencies": {
    "axios": "^1.4.0",
    "feedparser": "^3.0.0",
    "jsdom": "^22.1.0",
    "nodemailer": "^6.9.4"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.34.0"
  }
}
